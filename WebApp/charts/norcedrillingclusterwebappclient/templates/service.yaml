apiVersion: v1
kind: Service
metadata:
  name: {{ include "norcedrillingclusterwebappclient.fullname" . }}
  labels:
    {{- include "norcedrillingclusterwebappclient.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "norcedrillingclusterwebappclient.selectorLabels" . | nindent 4 }}
